## Работа с тегами (git tag)

Теги используются для пометки определённых коммитов (например, выпусков версий).

### Примеры:
1. Создание тега:
```bash=
git tag v1.0
```
2. Отправка тегов в удалённый репозиторий:
```bash=
git push origin --tags
```
[К содержанию](readme.md)